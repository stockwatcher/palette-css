
Palette.css - A Lightweight Material Design Toolkit

User Installation Instructions:

```
app.use(express.static(__dirname + '/node_modules/palette-css'), {
  index: false
});
```

Dev Installation Instructions:

npm install

sudo gem install listen
sudo gem install sass

sass --watch sass:gen/css

